/*
*   Query Builder
*/

var queryBuilder = {
    buildAll: buildAll
};

function buildAll(allAccts) {
    //  define local variables
    var allAccts = [];

    return allAccts;
}

module.exports = queryBuilder;